services:
  - type: web
    name: receipt-reward
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn rrs:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0